init('', 'Matrikon.OPC.Simulation')

logInfo('script demonstrates aysnchronous writing then reading for an item')

group('test_group').with
{
	item('testGroup.myBool')
	for(i in 1..10)
	{
		assertAsyncEquals('checking async update value', 1000, 'beouf!', 'this.that.the.other')
		item('testGroup.myString').asyncValue = "async value [${i}]"
		logInfo("loop counter ${i}")
	
		
		sleep(10000)
	}
	
	
}

logInfo('end of script')