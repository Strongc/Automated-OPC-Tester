========================================================================
    CONSOLE APPLICATION : ToolkitDriverTestApp Project Overview
========================================================================

App should not form part of any application - is a stand alone application for driving the OPCClientToolkitWrapper dll
from a console application. This is a TEST ONLY application which loads the dll, performs some operations then unloads
the dll and exits.